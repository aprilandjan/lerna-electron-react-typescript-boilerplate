{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // path alias
    "baseUrl": "src",
    "paths": {
      "@/*": ["*"],
      "@src/*": ["*"],
      //  can define multiple different alias paths
      "@utils/*": ["utils/*"]
    }
  }
}
