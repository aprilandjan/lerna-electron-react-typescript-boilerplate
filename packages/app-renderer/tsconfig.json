{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // path alias
    "baseUrl": "src",
    "paths": {
      "@/*": ["*"],
      //  can define multiple different alias paths
      "@utils/*": ["utils/*"]
    }
  }
}
