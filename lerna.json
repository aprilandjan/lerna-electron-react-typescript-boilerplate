{
  "version": "1.0.0",
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "conventionalCommits": true,
      "message": "chore(release): publish %s"
    }
  }
}
